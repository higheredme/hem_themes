

1977 Openbox : obt
dalisha : archive
mistral : lien mort (deviantart) , github pour gtk3
OrangeBlue box : archive
nightmare : déja installé
pixelstyle : archive tar
faenza :  tar gz
windows 8 phone colours OB theme : archive zip
color ui:  zip + wallpaper

numix : apt-get
sudo add-apt-repository ppa : numix/ppa
sudo apt-get update
sudo apt-get install numix-icon-theme-circle

orta:
sudo add-apt-repository ppa:nikount/orta-desktop
sudo apt-get update
sudo apt-get install orta-theme
To install the emerald decorators :
	sudo apt-get install orta-emerald-decorators
To install the Xfwm4 decorators :
	sudo apt-get install orta-xfwm4-decorators

numix gtk3 : 
	sudo add-apt-repo
	sitory ppa:numix/ppa
	sudo apt-get update && sudo apt-get install numix-gtk-theme

Ferret gtk3 :
	sudo apt-get install gtk2-engines-murrine

Dust: 
	archive tar gz

Ambiance & Radiance flat colors:
	sudo add-apt-repository ppa:ravefinity-project/ppa
	sudo apt-get update
	sudo apt-get install ambiance-flat-colors radiance-flat-colors

Zuki: archive zip

Installation pour dézipper:

sudo apt-get install p7zip

